#!/bin/sh
# i3blocks config — gruvbox-dark themed to match the i3 bar colors in
# Desktop/i3/conf.d/30-autostart.conf

separator_block_width=15
markup=none

[disk]
label=🖴 
command=df -h /home | awk 'NR==2{print $4" free"}'
interval=300

[volume]
command=$HOME/.config/i3blocks/volume-scroll
interval=once
signal=1

[calendar]
label=🗓 
command=date '+%a %d %b'
interval=1

[time]
label=⏲ 
command=date '+%H:%M '
interval=1

[pin-bar]
command=$HOME/.config/i3blocks/pin-bar
interval=1
